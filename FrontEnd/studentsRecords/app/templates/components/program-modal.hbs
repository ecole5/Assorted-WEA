<!-- This is the template for name-code, it is a modal that has a varying number of editable,
field. Each field has a delete buttton. Fields are populated with code names. There is an add button
to add a new field -->

<div class="ui modal">
  <div class="header">Program</div>
  <div class="content">
    <div class="ui grid">
      <div class="fifteen wide column">
        <div class="actions">
          <div class="ui form">

              <div class="five fields">
                <div class="field">
                     <div class="ui label">
                  Name
                </div>
                </div>
                <div class="field">
                     <div class="ui label">
                  Level
                </div>
                </div>
                <div class="field">
                     <div class="ui label">
                  Load
                </div>
                </div>
                <div class="field">
                     <div class="ui label">
                  Status
                </div>
                </div>
              </div>




              <!--Editing option for each name -->
              {{#each codeModel as |codeChoice|}}
              <div class="five fields">

                <!--On select will set target model context, but onblur will pass the value of the field -->
                <div class="field">
                  <div class="ui input">
                    <input type="text" value={{codeChoice.name}} onfocus={{action 'setCurrentModel' codeChoice}} onblur={{action
                      'editName' value="target.value" }}>
                  </div>
                </div>

                <!--On select will set target model context, but onblur will pass the value of the field -->
                <div class="field">
                  <div class="ui input">

                    <input type="number" min="1" max="6" step="1"value={{codeChoice.level}} onfocus={{action 'setCurrentModel' codeChoice}} onblur={{action
                      'editLevel' value="target.value" }}>
                  </div>
                </div>
                <!--On select will set target model context, but onblur will pass the value of the field -->
                <div class="field">




                  <div class="ui input">

                    <input type="number" min="0" max="7" step="0.5"  value={{codeChoice.load}} onfocus={{action 'setCurrentModel' codeChoice}} onblur={{action 'editLoad'
                      value="target.value" }}>
                  </div>
                </div>
                <!--On select will set target model context, but onblur will pass the value of the field -->
                <div class="field">
                  <div class="ui input">

                    <input type="text" value={{codeChoice.status}} onfocus={{action 'setCurrentModel' codeChoice}} onblur={{action 'editStatus'
                      value="target.value" }}>
                  </div>
                </div>

                <!--Remove Button -->
                <div class="field">
                  <button class="ui icon button" {{action 'removeCode' codeChoice }}>
                <i class="remove circle icon"></i>
                </button>
                </div>


                <!-- end of list element !-->
              </div>
              {{/each}}

              <!-- end of entire list!-->

              <!-- Add button !-->
              <button class="ui button" {{action 'newCode'}}>
     <i class="plus icon"></i>
  </button>

          
          </div>
        </div>
      </div>
      <div class="six wide column">
      </div>
    </div>

    <div class="actions">
      <br>
      <hr>
      <!-- Close Button !-->
      <div class="ui button" {{action "exit"}}>Ok</div>
    </div>

  </div>
</div>